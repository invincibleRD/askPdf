apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app.kubernetes.io/part-of: askpdf

resources:
  - serviceaccount.yaml
  - config.yaml
  - api.yaml
  - worker.yaml
  - ingress.yaml
  - networkpolicy.yaml
  - index-migration.yaml

images:
  - name: askpdf
    newTag: latest
