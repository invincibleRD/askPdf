apiVersion: v1
kind: ServiceAccount
metadata:
  name: askpdf
  # The GCP overlay annotates this for Workload Identity, which is what lets
  # the pods reach the bucket with no service account key on disk.
