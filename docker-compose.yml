# ---------------------------------------------------------------------------
# Local development stack.
#
#   docker compose up -d          # api + worker + mongo + redis
#   docker compose logs -f worker
#   docker compose down -v        # also drops the data volumes
#
# Mirrors the Kubernetes topology in deploy/k8s: the API and the worker are
# separate services running the same image, so a bug that only shows up when
# the two are split shows up here too.
# ---------------------------------------------------------------------------

name: askpdf

x-app-env: &app-env
  # Everything comes from .env so the containers run the same configuration the
  # host tooling does; only the service addresses differ.
  NODE_ENV: development
  # JSON logs, as in production — and pino-pretty isn't in the runtime image.
  LOG_PRETTY: 'false'
  MONGO_URI: mongodb://mongo:27017/askpdf
  REDIS_URL: redis://redis:6379
  STORAGE_LOCAL_PATH: /app/storage
  GCS_KEY_FILE: /app/.secrets/${GCS_KEY_FILENAME:-gcs-key.json}

x-app-base: &app-base
  build:
    context: .
    target: runtime
  image: askpdf:dev
  restart: unless-stopped
  env_file:
    - .env
  depends_on:
    mongo:
      condition: service_healthy
    redis:
      condition: service_healthy
  volumes:
    # Bind-mounted source with node_modules masked, so an edit restarts the
    # process without a rebuild.
    - ./src:/app/src:ro
    - ./.secrets:/app/.secrets:ro
    - documents:/app/storage
  networks: [askpdf]

services:
  api:
    <<: *app-base
    container_name: askpdf-api
    command: ['node', '--watch', 'src/entrypoints/api.js']
    environment:
      <<: *app-env
      PORT: '3000'
    ports:
      - '${API_PORT:-3000}:3000'

  worker:
    <<: *app-base
    container_name: askpdf-worker
    command: ['node', '--watch', 'src/entrypoints/worker.js']
    environment:
      <<: *app-env
      WORKER_CONCURRENCY: ${WORKER_CONCURRENCY:-2}
    ports:
      - '${WORKER_METRICS_PORT:-9100}:9100'
    # The worker serves no HTTP traffic, so the image healthcheck does not
    # apply to it.
    healthcheck:
      disable: true

  mongo:
    image: mongo:7
    container_name: askpdf-mongo
    restart: unless-stopped
    command: ['--bind_ip_all']
    environment:
      MONGO_INITDB_DATABASE: askpdf
    ports:
      - '${MONGO_PORT:-27017}:27017'
    volumes:
      - mongo-data:/data/db
    healthcheck:
      test: ['CMD', 'mongosh', '--quiet', '--eval', "db.adminCommand('ping').ok"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    networks: [askpdf]

  redis:
    image: redis:7-alpine
    container_name: askpdf-redis
    restart: unless-stopped
    # Jobs are durable state, not a cache: appendonly keeps a queued upload
    # alive across a restart.
    command: ['redis-server', '--appendonly', 'yes', '--maxmemory-policy', 'noeviction']
    ports:
      - '${REDIS_PORT:-6379}:6379'
    volumes:
      - redis-data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 3s
      retries: 10
      start_period: 5s
    networks: [askpdf]

volumes:
  mongo-data:
  redis-data:
  documents:

networks:
  askpdf:
    driver: bridge
